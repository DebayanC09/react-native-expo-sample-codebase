import {StyleSheet} from "react-native";

export const AddUpdateTodoScreenStyle = StyleSheet.create({
  scrollContainer: {
    paddingHorizontal: 16,
    gap: 32,
  },
  formContainer: {gap: 16},
});
